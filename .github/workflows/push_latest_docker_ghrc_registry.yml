name: "Push Latest Docker Image to GHCR Registry"

on:
  push:
    branches:
      - main
      - master
      - develop

jobs:
  build:
    uses: ./.github/workflows/docker-build-push.yml
    with:
      image_tag: "latest"
    secrets: inherit