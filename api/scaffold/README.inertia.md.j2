# {{ app_name }} — Laravel + Inertia.js ({{ starter_kit | replace('-', ' ') | title }})

Generated by **Laravel Dockerized Installer**.

## Quick start

```bash
# 1. Extract the archive
unzip laravel-{{ app_name | lower | replace(' ', '-') }}-inertia-{{ starter_kit }}-docker.zip
cd {{ app_name | lower | replace(' ', '-') }}

# 2. Build and start the containers
docker compose up --build
```

App: `http://localhost:{{ app_port }}`  
Vite dev server: `http://localhost:5173`

> **This archive is fully ready to use.** The project was created with `laravel new`
> on the server — an `APP_KEY` is already set in `.env` and all Docker files are
> pre-configured.

## What's inside this archive

| File / Folder | Purpose |
|---|---|
| `app/`, `config/`, `routes/`, … | Full Laravel project source |
| `Dockerfile` | PHP {{ php_version }}-cli image for the Laravel backend |
| `docker-compose.yml` | App + Vite (Node 20) + {% if db != 'sqlite' %}{{ db }} + {% endif %}Redis |
| `.env` | Pre-filled environment with a generated `APP_KEY` |
| `.env.docker` | Backup copy of the Docker environment file |

## Project configuration

| Option | Value |
|---|---|
| Starter kit | {{ starter_kit | replace('-', ' ') | title }} |
| Auth provider | {% if auth_provider == 'workos' %}WorkOS AuthKit{% else %}Laravel built-in (Breeze){% endif %} |
| Teams | {{ 'yes' if teams else 'no' }} |
| Testing | {{ testing_framework | upper }} |
| Database | {{ db }} |
| PHP version | {{ php_version }} |
{% if install_boost %}| Laravel Boost | yes |
{% endif %}

## Useful commands

```bash
# Stop containers
docker compose down

# Tail app logs
docker compose logs -f app

# Open a shell in the app container
docker compose exec app bash

# Run migrations
docker compose exec app php artisan migrate
```

## Stack

| Service | Image | Host port |
|---|---|---|
| app | php:{{ php_version }}-cli-alpine | {{ app_port }} |
| vite | node:20-alpine | 5173 |
{% if db == 'mysql' %}| db | mysql:8.0 | 3306 |
{% elif db == 'postgres' %}| db | postgres:16-alpine | 5432 |
{% endif %}| redis | redis:7-alpine | 6379 |

