# {{ app_name }} — Laravel {{ laravel_version }} (Docker)

Generated by **Laravel Dockerized Installer**.

## What's inside

| File | Purpose |
|---|---|
| `Dockerfile` | PHP {{ php_version }}-fpm image for the Laravel app |
| `docker-compose.yml` | App + {% if db != 'sqlite' %}database ({{ db }}) + {% endif %}Redis stack, app exposed on port {{ app_port }} |
| `.env.docker` | Pre-filled environment variables for Docker |

## Quick start

```bash
# 1. Build and start the containers
docker compose up --build
```

The application will be available at **http://localhost:{{ app_port }}**.

> **This archive is ready to run.** An `APP_KEY` was generated server-side and is
> already set in `.env`. No manual key generation or file copying is required.

## First-time database setup

```bash
# Run migrations (inside the running app container)
docker compose exec app php artisan migrate
```

## Useful commands

```bash
# Start (detached)
docker compose up -d

# Stop
docker compose down

# Tail logs
docker compose logs -f app

# Open a shell in the app container
docker compose exec app bash
```

## Stack

| Service | Image | Port |
|---|---|---|
| app | php:{{ php_version }}-fpm | — |
{% if db == 'mysql' %}| db | mysql:8.0 | 3306 |
{% elif db == 'postgres' %}| db | postgres:16-alpine | 5432 |
{% endif %}| redis | redis:7-alpine | 6379 |
| nginx | nginx:alpine | {{ app_port }} → 80 |
